{
  "name": "mole-lab",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test"
  }
}
